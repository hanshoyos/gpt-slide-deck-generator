{
  "name": "pptx-slide-deck-generator",
  "version": "1.0.0",
  "description": "Standalone web app for generating PowerPoint files from text scripts.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",          // Web server framework
    "body-parser": "^1.20.2",      // Middleware to parse incoming request bodies
    "pptxgenjs": "^3.11.0"         // PowerPoint generation library
  },
  "engines": {
    "node": ">=16"                  // Ensure the server runs with Node.js version 16+
  }
}
